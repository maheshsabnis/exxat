// explicit datatype declarations (recommended)
let n1:number = 100;
// implicit declaration based on intiail value
let n2 = 100; // n2 will be number by default

let name1:string;
name1='TypeScript Training';
// upper case
console.log(name1.toUpperCase());
// lower case
console.log(name1.toLowerCase());
// get length
console.log(name1.length);
// concatination with template string
let name2='At Exxat Systems';
console.log(`${name1} ${name2}`);

